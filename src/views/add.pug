extends templates/layout

block content
  h1=pageTitle
  form(method="POST")
    input(name="title", placeholder="Title",required)
    input(name="synopsis", placeholder="Synopsis",required)
    input(name="genres", placeholder="Genres",required)
    input(type="submit", value="Add Movie")